# This is an example connection.cfg file. Fill in the blank, and rename it to 'connection.cfg'
# MetagenomeDB needs to know the IP address of the MongoDB server, as well as the port and the name of the database.

[connection]
host: localhost
port: 27017
database: YOUR DATABASE NAME
